var ReactDOM = require('react-dom');
var React = require('react');
var ArticleApp = require('./components/ArticleApp.jsx');

ReactDOM.render(
    <ArticleApp />,
    document.getElementById('mount-point')
);
